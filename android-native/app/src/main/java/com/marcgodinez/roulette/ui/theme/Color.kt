package com.marcgodinez.roulette.ui.theme

import androidx.compose.ui.graphics.Color

val DarkBg = Color(0xFF050510)
val SurfaceBg = Color(0xFF12121A)
val ElevatedBg = Color(0xFF1E1E28) // New for popups/cards

// Brand / Accent
val PrimaryGold = Color(0xFFFCD34D) // Neon Gold
val AccentGold = PrimaryGold
val SecondaryGold = Color(0xFFF59E0B) // Accent Hover
val AccentBlue = Color(0xFF3B82F6) // Cyber Blue
val AccentPurple = Color(0xFFA855F7) // Neon Purple

// Betting Colors
val BetRed = Color(0xFFFF1111) // Neon Red
val BetBlack = Color(0xFF111111) // Deep Black
val BetGreen = Color(0xFF00FF00) // Toxic Neon Green

// Status
val SuccessGreen = Color(0xFF22C55E)
val ErrorRed = Color(0xFFEF4444)
val WarningYellow = Color(0xFFEAB308)

// Text
val TextWhite = Color(0xFFFFFFFF)
val TextGray = Color(0xFF94A3B8)
val TextMuted = Color(0xFF475569)

// Borders
val BorderSubtle = Color(0x1AFFFFFF) // 10% White
val BorderAccent = PrimaryGold

val AdBlue = Color(0xFF2196F3) // Keep existing if used
val CardBg = SurfaceBg // Map CardBg to SurfaceBg for consistency
